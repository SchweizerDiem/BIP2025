// Create Event for obj_game_manager

// Initialize the global gold variable
global.player_gold = 200; // Start the player with 200 gold (or any amount)

// Initialize any other global variables here (e.g., global.player_health, global.score)
// global.player_health = 100;